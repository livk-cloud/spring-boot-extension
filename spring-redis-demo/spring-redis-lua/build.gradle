apply plugin: 'java-library'
apply plugin: 'org.springframework.boot'

dependencies {
    api project(':spring-redis-demo:spring-redis-common')
    api 'org.springframework.boot:spring-boot-starter-web'
}

test {
    useJUnitPlatform()
}
