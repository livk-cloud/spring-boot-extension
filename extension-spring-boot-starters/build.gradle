plugins {
    id "com.livk.root"
    id "java-library"
}

subprojects {
    dependencies {
        api project(":extension-spring-boot-autoconfigure")
        api "org.springframework.boot:spring-boot-starter"
    }
}
